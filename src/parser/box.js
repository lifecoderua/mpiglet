function Box(buffer, offset) {
  this.buffer = buffer;
  this.offset = offset;

  // TODO: read from buffer
  // this.length = length;
  // this.type = type;
  // this.nextBoxOffset
}

Box.prototype.getContents = function() {

};

export default Box;
